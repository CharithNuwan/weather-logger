{
  "name": "weather-logger",
  "version": "1.0.0",
  "description": "ESP32 BME280 Weather Logger",
  "dependencies": {
    "@libsql/client": "^0.6.0" 
    "builds": [
        {
            "src": "index.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "index.js"
        }
    ]
}
